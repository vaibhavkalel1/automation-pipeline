kind: Deployment
apiVersion: apps/v1
metadata:
  name: poll-automationapp-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poll-automationapp
  template:
    metadata:
      name: pollpod
      labels:
        app: poll-automationapp
    spec:
      containers:
      - name: automationcontainer
        image: vaibhavkalel/pipelineimage:latest
        command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
        ports:
        - containerPort: 8000
